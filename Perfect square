#include<stdio.h>
int main()
{
	int x, m, n, i, j;
	for (i = 1; i <= 168 / 2; i++)
	{
		if (168 % i == 0) j = 168 / i;
	}
	n = (i + j) / 2;
	m = (i - j) / 2;
	x = m * m - 100;
	printf("x=%d\n", x);
	/*一个整数，它加上100后是一个完全平方数，
	再加上168又是一个完全平方数，请问该数是多少？*/
	return 0;
}
