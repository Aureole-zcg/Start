#include<stdio.h>
int main()
{
	int a[10];
	int i, max = 0;
	printf("请输入10个整数:");
	for (i = 0; i < 10; i++)
		scanf("%d", &a[i]);
	max = a[0];
	for (i = 1; i < 10; i++)
		if (a[i] > max)
			max = a[i];
	printf("the max is %d\n", max);
	return 0;
}
